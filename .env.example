# Frontend / Vite — read at `npm run dev` / `npm run build` / `npm run tauri:dev`.
# Copy to .env and fill in. The placeholder below is an invalid Clerk key; it's
# just there to show the expected format.

VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace-me
VITE_RADAR_SERVER_URL=http://127.0.0.1:8787
